info:
  name: GET user by query id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/greeting?id={{user_id}}"
  params:
    - name: id
      value: "{{user_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
